{
  "name": "cartograph",
  "private": true,
  "version": "1.0.0",
  "description": "Cartograph sitemap generator — frontend + backend for Render",
  "scripts": {
    "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm install --prefix backend && node scripts/install-playwright-if-needed.js",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
